export const isByPassRoutes=[
    "/api/polar/webhook",
    "/api/inngest(.*)",
    "/api/auth",
    "/convex(.*)",
]
export const isProtectedRoutes=[
    "/dashboard(.*)",
]
export const isPublicRoutes=[
    "/auuth(.*)",
    "/",
]